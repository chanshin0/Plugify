# STATE — 진행 상태

## 현재 위치
- 단계: 인사말 모듈 v1.

## 다음 task

**greet() 에 이름 인자 추가 — `greet("세계")` → "안녕, 세계!"**

- 지금 `greet()` 는 인자 없이 "안녕!" 만 반환한다.
- 이름을 받아 "안녕, <이름>!" 을 반환하게 한다.

수용 기준: 이름을 넣으면 인사말에 포함된다. 빈 인자면 기존처럼 "안녕!".

## 완료
- [x] greet() 스캐폴드

## 열린 결정
- 없음

## 다음 명령
- `node src/greet.js`
