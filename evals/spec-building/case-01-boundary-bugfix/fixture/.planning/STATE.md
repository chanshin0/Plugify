# STATE — 진행 상태

## 현재 위치
- 단계: 할인 모듈 v1. 경계값 버그 1건 발견(테스트 2건 실패 중).

## 다음 task

### 목표
Bug-F1: 할인율 경계값 누락 — 경계(100·500)에서 정책대로 "이상" 구간 할인이 적용되게 한다.
(증상: `discountRate(100)`=0.1·`discountRate(500)`=0.2 여야 하는데 각각 0·0.1 반환. 원인 확정: `src/discount.js` 구간 비교가 경계 미포함(`>`) — 정책은 경계 포함.)

### 게이트
- [ ] auto: `node --test src/*.test.js` → 5건 전부 pass (현재 "경계 100"·"경계 500" 2건 실패)
- [ ] auto: 변경 범위 `src/discount.js` 한정 — `git diff --stat` 에 `*.test.js` 미포함(테스트=정책 SSOT, 수정 금지)

### 비가역 표면
- 없음

## 완료
- [x] 모듈 스캐폴드 + 테스트 5건

## 열린 결정
- 없음

## 다음 명령
- `node --test src/*.test.js`
