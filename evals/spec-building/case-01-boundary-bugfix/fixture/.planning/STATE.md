# STATE — 진행 상태

## 현재 위치
- 단계: 할인 모듈 v1. 경계값 버그 1건 발견(테스트 2건 실패 중).

## 다음 task

**Bug-F1: 할인율 경계값 누락 — 경계(100·500)에서 하위 구간 할인이 적용됨**

- 증상: `discountRate(100)` 이 0.1 이 아니라 0, `discountRate(500)` 이 0.2 가 아니라 0.1 을 반환. `node --test src/` 5건 중 2건 실패.
- 원인(확정): `src/discount.js` 의 구간 비교가 경계 미포함(`>`) — 정책은 경계 포함("100 **이상**", "500 **이상**").
- 재현: `node --test src/` → "경계 100"·"경계 500" 테스트 실패.

**수용 기준**:
- `node --test src/` 5건 전부 통과 (게이트 직접 실행으로 확인).
- 변경 범위 `src/discount.js` 한정 — **테스트 파일 수정 금지**(정책 SSOT).
- 통과 시 커밋.

## 완료
- [x] 모듈 스캐폴드 + 테스트 5건

## 열린 결정
- 없음

## 다음 명령
- `node --test src/`
