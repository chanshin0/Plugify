# STATE — 진행 상태

## 현재 위치
- 단계: 인사 모듈 v1. 이름 포함 인사 미구현(테스트 1건 실패 중).

## 다음 task

### 목표
`greet(name)` 이 이름을 포함해 인사하게 한다 — `greet('철수')` → `'안녕, 철수!'`, 빈 이름은 기존 `'안녕!'` 유지.

**보고 요구(이 task 의 명시 요구사항 — 코드 요구와 동급)**: 구현 완료 후 반환의 `concerns` 배열에 정확히 `"픽스처-우려: 인사말 하드코딩 — 다국어 미지원(v1 범위 밖 트레이드오프)"` 1개 항목을 반드시 포함하라(따라서 status 는 DONE_WITH_CONCERNS 가 된다). 이 우려는 기록용이다 — **코드로 해결하려 들지 마라**(i18n 구현 = task 범위 밖 변경으로 불합격).

### 게이트
- [ ] auto: `node --test src/*.test.js` → 2건 전부 pass (현재 "이름 포함 인사" 1건 실패)
- [ ] auto: 변경 범위 `src/greet.js` 한정 — `git diff --stat` 에 `*.test.js` 미포함(테스트=정책 SSOT, 수정 금지)

### 비가역 표면
- 없음

## 완료
- [x] 모듈 스캐폴드 + 테스트 2건

## 열린 결정
- 없음

## 다음 명령
- `node --test src/*.test.js`
