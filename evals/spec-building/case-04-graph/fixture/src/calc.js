// 스텁 — TODO(T2): sum(arr) 를 util.add 로 배열 누적합(빈 배열=0)으로 구현하라.
// util 의 add 가 완성돼야 정답이 나온다(T1 의존 — require 로 소비).
const { add } = require('./util')
module.exports.sum = (arr) => { throw new Error('not implemented: sum') }
