# RESULT — 2026-08-31

- 실제 관찰: 07·08편 제목·본문 RTF를 01–06 게시 정본과 비교했다. 기존 후보는
  본문 구간 부제목 0개였고, 정정 후보는 `[부제목]` 9개를 포함한다.
- 대본 보존: 실제 07 승인 대본 SHA-256
  `0c48f49fdc1857fc78efb6b76289f59eed2020484a1ccc4b0c5f28fe8772a695`를
  사용했다. 부제목 제거 시 원문 문구·순서·앞뒤 개행이 그대로 복원된다.
- 산출 검증: TXT SHA-256 `7e630c3bb25bb7089cb761dbe8b077a4c141594eec8eb343d8d1da1f335546b3`,
  RTF SHA-256 `9c6be407450f182d39036dec4c566230b254dc38fcccab19575f936026bab39f`.
  Windows RichTextBox로 RTF를 다시 평문화해 TXT와 일치함을 확인했다.
- 원격 관찰: Drive의 같은 파일 ID를 유지해 RTF를 교체했고, DriveFS 재읽기
  크기 37,456 bytes와 SHA-256이 로컬 후보와 일치했다. 폴더는 기존 13개
  exact child를 유지했다.
- 독립 검토: 첫 검토에서 `.strip()` 때문에 끝 개행이 보존 검사에서 빠지는
  blocker를 발견했다. raw text 보존으로 수정한 뒤 재검토에서 PASS, issues 0.
- 당시 결정적 검사: `python3 check-skill.py` PASS,
  `sha256sum -c FROZEN.sha256` PASS.
- 첫 실전 관찰 판정: PASS. 이후 09·10편 첫 게시 문안에서도 같은 gate를 다시
  관찰하고, 부제목 0개·대본 raw text 불일치를 실패시키는지 확인한다.

## 같은 날 사용자 출력 형식 정정 재관찰

- 요구 변경: 앞으로 RTF를 쓰지 않고 UTF-8 TXT만 전달한다. 각 부제목과 바로
  앞 본문 문장 사이의 빈 줄은 1줄이 아니라 정확히 3줄이어야 한다.
- 시스템 반영: 스킬과 후속편 reference가 TXT 단일 역할·RTF 금지·빈 줄 3줄
  (개행 문자 4개)을 명시하도록 고쳤다. 이 사용자 지시로 기존 confirmed
  CASE의 RTF 요구를 대체하고 fresh/blind 재검토 대상으로 열었다.
- 첫 재검토는 기존 RTF·빈 줄 1줄 지시가 새 문구와 공존해도 checker가 통과할
  수 있는 문제를 발견했다. checker에 구형 상충 지시 금지 검사를 추가했다.
- 첫 실전 관찰: `godowon-office`의 07·08 정정본은 TXT와 receipt만 만들었고
  RTF는 없다. 아홉 부제목 모두 직전 개행 문자가 4개이며 TXT SHA-256은
  `fcd0f96dcb38b545d76fa48ea048e2bb181f29a1438cfb7723b278a635ecfb1c`다.
- 외부 경계: 새 TXT의 Drive 업로드·기존 RTF 교체는 실행하지 않았다.
- 재검토 판정: 금지 검사를 추가하고 CASE·ANSWER·RUN·checker 해시를 다시
  동결했다. 같은 fresh/blind reviewer가 `check-skill.py` PASS와
  `sha256sum -c FROZEN.sha256` 4/4 OK를 독립 재실행하고 기존 두 이슈가 모두
  닫혔다고 판정했다.
