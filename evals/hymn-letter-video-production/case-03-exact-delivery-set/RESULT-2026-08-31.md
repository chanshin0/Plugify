# 2026-08-31 검증 기록 — 실제 07·08 Drive 납품 사고

## 수정 전 증거

- Drive의 07·08 MP4는 외부 SRT 방식의 이전 파일이었고, 로컬 visual QC를
  통과한 고정자막 MP4와 SHA-256이 달랐다.
- 썸네일 PNG 10개만 원격 ID·크기·해시를 검증한 보고가 전체 요청의 완료처럼
  해석될 수 있었다. 실제 관찰은 `pre-fix-observation.md`에 고정했다.
- 합성 negative control은 checker에서 예상대로 FAIL했다. 실제 사고 관찰과
  합성 대조군을 혼동하지 않는다.

## 독립 평가 동결

- fresh/blind reviewer가 CASE/ANSWER/checker/fixture를 스킬 수정 전에 검토했다.
- 첫 검토에서 빈 remote inventory와 episode ID만으로 통과하는 Goodhart 허점을
  발견해 exact identity 검증으로 보강했다.
- 두 번째 검토에서 공개 CASE의 `remote_id` 요구 누락을 발견해 계약을 맞췄다.
- 최종 검토는 negative/underspecified/remote-ID omission/SHA mutation/stale relabel
  모두 FAIL, fixture-derived positive만 PASS임을 확인했다.
- 그 뒤 `FROZEN.sha256`을 만들고 `evals/confirmed-cases.txt`에 등록했다.

## 시스템 수정과 첫 실전 관찰

- 찬송편지 스킬에 편·역할·파일명·크기·SHA 기반 exact delivery manifest와
  `PARTIAL_STALE_TARGET` 규칙을 추가했다.
- `scripts/verify_delivery_set.py`는 manifest hash, 전체 역할 집합, exact payload,
  원격 ID/크기/DriveFS 해시, stale cleanup과 exact target children을 검증한다.
- 실제 교체 작업에서 기존 17개 remote ID가 trashed/tombstoned임을 확인하고
  `찬송편지_07-08편` 폴더에 고정자막 MP4 2개와 PNG 10개만 올렸다.
- 실제 manifest/receipt로 새 verifier를 실행해 `status=PASS`, folder remote ID
  `1j5eGCcKsVvTnIjnKplqTOxqySWpEsAcS`, payload 12개를 확인했다. 이것을 시스템
  수정 후 첫 실전 관찰로 기록한다.

