# Plugify — 본사 모드 (루프를 만드는 레포)

이 레포에서 열린 세션은 **본사 모드**다: 제품이 아니라 **공정(스킬·에이전트·워크플로우)** 을 만들고 고친다. 지점(프로젝트 레포) 세션은 마지막 커밋 버전의 공정을 소비한다 — **커밋 = 출시**.

## 설계 원칙
- **속으면 안 되는 판정은 코드, 상황 판단은 지시문**: 커밋 실재·게이트 통과 같은 "거짓 보고가 치명적인" 판정은 workflow.mjs 등 결정적 코드로 내린다. 전략 분기·예외 판단은 SKILL.md 지시문에 둔다.
- **lean-agent-design**: 메인=오케스트레이터(컨텍스트 신선도 1순위), 구체 작업은 격리 에이전트. 함대 금지(역할당 1).
- **에이전트 SSOT = dual-block** (`claude:`+`codex:` frontmatter, `skills/*/agents/*.md`·`agents/*.md`). 변경 시 `scripts/install.sh` 재실행 — agentType 레지스트리는 세션 재시작 후 실효.
- **가지치기 의무**: 룰 추가 전에 기존 룰과 겹침 확인 — 겹치면 통합. 사고마다 쌓기만 하면 SKILL 비대화 = 컨텍스트 오염.

## 출하 조건 (스킬/에이전트/워크플로우를 만들거나 고칠 때)
1. **evals 문제집 통과** — `evals/<스킬>/` 케이스가 있으면 전부 실행해 ANSWER 채점표 합격. 없으면 케이스를 만들거나, 비대상 사유(순수 문서 변경 등)를 커밋 메시지에 명시.
2. **canary 선언** — 출하 후 첫 실전 실행 1회를 관찰 대상으로 명시(그 실행이 변경을 실증해야 사고/작업이 닫힘). 정본 = `SYSTEM.md §3.1` 불릿(닫기 = 사람이 줄 제거).
3. 워크플로우 스크립트 변경은 최소 구문검증(AsyncFunction 래핑, **`export const meta` 라인은 제거 후 래핑** — `node --check` 는 top-level return 때문에 부적합).
4. git 커밋(한국어, 무엇이 왜). `--no-verify`·`--force` 금지.

## 사고 처리
`incident-protocol` 스킬 절차를 따른다. 핵심: 사과로 종결 금지, 방지책은 "그 행동을 만든 자산"에 직접 반영, **사고를 재현하는 회귀 케이스를 `evals/` 문제집에 추가**(문제집이 두꺼워지는 것이 본사 루프의 연료), 메모리는 사례 기록만.

## 본사 루프의 종료조건
"좋은 공정"은 모호하다 — **문제집 통과가 각 사이클의 종료조건**이다. 출제(케이스·합격선)는 사람/메인이 정하고 confirm 받는다. **공정을 고치는 에이전트가 합격선을 완화하는 방향의 수정 금지**(굿하트). 규약 상세 = `evals/README.md`.
